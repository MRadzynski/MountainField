import classes from '../../styles/components/loader.module.scss';

const Loader = () => (
  <div className={classes.loaderContainer}>
    <div className={classes.loaderElement} />
  </div>
);

export default Loader;
